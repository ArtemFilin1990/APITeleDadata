# Legacy ENV reference file (kept for backward compatibility).
# Use `.env.example` or templates in `env/` for actual setup.

# Required
TELEGRAM_BOT_TOKEN=your-telegram-bot-token   # alias: BOT_TOKEN
DADATA_API_KEY=your-dadata-api-key           # alias: DADATA_TOKEN

# Optional (required for MCP mode)
DADATA_SECRET_KEY=your-dadata-secret-key     # alias: DADATA_SECRET
OPENAI_API_KEY=your-openai-api-key

# Runtime mode
MODE=polling
WEBHOOK_URL=https://your-domain.example/tg/webhook
PORT=8080

# Startup retry options
BOT_STARTUP_MAX_RETRIES=5
BOT_STARTUP_RETRY_BASE_DELAY_SECONDS=2
BOT_STARTUP_RETRY_MAX_DELAY_SECONDS=30
