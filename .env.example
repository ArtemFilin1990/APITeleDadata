# Telegram
TELEGRAM_BOT_TOKEN=
# Альтернативный нейминг для совместимости:
# BOT_TOKEN=

# DaData (обязательно для прямого режима)
DADATA_API_KEY=
# Альтернативный нейминг для совместимости:
# DADATA_TOKEN=

# DaData secret (обязателен только для MCP-режима)
DADATA_SECRET_KEY=
# Альтернативный нейминг для совместимости:
# DADATA_SECRET=

# OpenAI (обязателен только для MCP-режима)
OPENAI_API_KEY=

# Режим запуска
MODE=polling
WEBHOOK_URL=
PORT=8080

# Опциональные ретраи старта бота при временных сетевых ошибках Telegram API
BOT_STARTUP_MAX_RETRIES=5
BOT_STARTUP_RETRY_BASE_DELAY_SECONDS=2
BOT_STARTUP_RETRY_MAX_DELAY_SECONDS=30

# Уровень логирования
LOG_LEVEL=INFO
