# --- Required (bot startup) ---
# Telegram token (BotFather)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# DaData API token (direct mode)
DADATA_API_KEY=your-dadata-api-key

# --- Optional (MCP mode) ---
# Required only for MCP/OpenAI flow
DADATA_SECRET_KEY=your-dadata-secret-key
OPENAI_API_KEY=your-openai-api-key

# --- Runtime mode ---
# polling | webhook
MODE=polling
WEBHOOK_URL=https://your-domain.example/tg/webhook
PORT=8080

# --- Logging ---
LOG_LEVEL=INFO

# --- Telegram startup retry tuning ---
BOT_STARTUP_MAX_RETRIES=5
BOT_STARTUP_RETRY_BASE_DELAY_SECONDS=2
BOT_STARTUP_RETRY_MAX_DELAY_SECONDS=30

# --- Aliases supported by config.py ---
# BOT_TOKEN -> TELEGRAM_BOT_TOKEN
# DADATA_TOKEN -> DADATA_API_KEY
# DADATA_SECRET -> DADATA_SECRET_KEY
